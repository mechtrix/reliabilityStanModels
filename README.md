# reliabilityStanModels

This is the repo to provide interactive session the ability to use precompiled Stan models.

install with:

```
remotes::install_github("mechtrix/reliabilityStanModels")
```

